apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  labels:
    app: inventory
    release: kube-prometheus
spec:
  selector:
    app: inventory
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5138