apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  labels:
    app: notifications
    release: kube-prometheus
spec:
  selector:
    app: notifications
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5221