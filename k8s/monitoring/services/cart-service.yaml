apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: cart
    release: kube-prometheus
spec:
  selector:
    app: cart
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5151