apiVersion: v1
kind: Service
metadata:
  name: payments-service
  labels:
    app: payments
    release: kube-prometheus
spec:
  selector:
    app: payments
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5195