apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  labels:
    app: shipping
    release: kube-prometheus
spec:
  selector:
    app: shipping
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5134