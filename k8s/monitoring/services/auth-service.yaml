apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth
    release: kube-prometheus
spec:
  selector:
    app: auth
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5292