apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app: orders
    release: kube-prometheus
spec:
  selector:
    app: orders
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 5059  